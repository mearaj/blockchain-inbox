import {makeStyles} from '@material-ui/core/styles';


const useStyles = makeStyles(() => {

  return {
    root: {},
    accountCard: {
      marginBottom: 12,
    },
  }
});

export default useStyles;
